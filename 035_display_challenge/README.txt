Using the stylesheet in this directory, create a page filled with divs styled by that style sheet. 

You will need to add one additional declaration to your stylesheet: 

change the default display property of divs (block) to a setting which allows the divs to be displayed one after another horizontally.